[package]
name = "erc20"
version = "0.1.0"
edition = "2021"

#[profile.dev]
#panic = "abort"

#[profile.release]
#panic = "abort"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
bytes = "1.9"
